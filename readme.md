# basic web server in c(basic crud todo) for windows

## How to run: 

1. Install and configure C compiler(mingw)
2. Clone and open the repo
3. compile the code: 
```bash
gcc server.c -o server.exe -lws2_32   # MinGW
```
4. Run the ./server.exe file
```bash
./server.exe
```