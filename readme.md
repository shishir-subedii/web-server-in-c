# basic web server in c(basic crud todo)

## How to run: 

1. Install and configure C compiler(mingw)
2. compile the code: 
```bash
gcc server.c -o server.exe -lws2_32   # MinGW
```
3. Run the ./server.exe file
```bash
./server.exe
```